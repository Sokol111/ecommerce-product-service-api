name: Generate and Release SDK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: Sokol111/ecommerce-infrastructure/.github/workflows/reusable-sdk-release.yml@master
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
