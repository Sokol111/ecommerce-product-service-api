name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: Sokol111/ecommerce-infrastructure/.github/workflows/generate-and-release-go-js-api.yml@master
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
